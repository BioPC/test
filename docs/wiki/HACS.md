# HACS notes

Home PV Control is not a normal Python Home Assistant integration.

It contains:

- Home Assistant package YAML
- Node-RED flow JSON
- dashboard YAML
- documentation

This means it cannot be installed like a normal integration unless the project is later restructured specifically for HACS.

For now, use this repository as a source of files and documentation.
